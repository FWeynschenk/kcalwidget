# Normalise to LF in the repository. gradlew in particular must stay LF or it fails with a
# bad interpreter on Linux and macOS.
* text=auto eol=lf

gradlew text eol=lf
*.bat text eol=crlf

*.png binary
*.jar binary
*.apk binary
